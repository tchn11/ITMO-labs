package messages;

/**
 * Status enum:
 * ERROR: Not fatal error that user can fix
 * FINE: All right
 * EXIT: Need to stop program
 */
public enum Status {
    ERROR,
    FINE,
    EXIT;
}
